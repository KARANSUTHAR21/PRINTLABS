Read AGENTS.md and inspect the existing PrintHub repository.

The planning phase is complete. Do not redesign the architecture or create a new plan.

Continue implementation from the current state of the repository.

First inspect the existing code and determine:
- what has already been implemented
- what is incomplete
- what is broken
- what the next implementation task should be

Then implement the next required functionality directly in the existing codebase.

Follow all requirements and architectural decisions in AGENTS.md.
Do not replace working code unnecessarily.
Do not create duplicate implementations.
Keep the application production-structured.

After implementation:
1. Run the relevant tests.
2. Fix any failures caused by your changes.
3. Report exactly what files were changed and what was implemented.